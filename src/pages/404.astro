---
import { siteMetadata } from '../site.config.js'
import Layout from '../layouts/Layout.astro'
---

<Layout title={`Page not found - ${siteMetadata.shortName}`}>
  <main>
    <h1>Page not found</h1>
  </main>
</Layout>

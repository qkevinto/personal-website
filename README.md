# personal-website
Kevin To's personal website, built using static site generator [Astro](https://astro.build/).

## Usage
* `npm run dev` to start Astro's dev server with hot-reloading.
* `npm run build` to build the site to `dist/`.
* `npm run preview` to preview the production build locally.
* `npm run deploy` to deploy to GitHub Pages.
